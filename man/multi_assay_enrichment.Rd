% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chemotype_enrich.R
\name{multi_assay_enrichment}
\alias{multi_assay_enrichment}
\title{Multi-Assay Chemotype Enrichment}
\usage{
multi_assay_enrichment(
  hitcalls,
  assays,
  toxprints,
  id_col_hitcalls,
  id_col_toxprints = id_col_hitcalls,
  endpoint_col_name = "assay",
  workers = 1L,
  ...
)
}
\arguments{
\item{hitcalls}{Tibble/dataframe with a chemical ID column and one column per assay containing hit calls (0/1).}

\item{assays}{Character vector of assay column names to calculate enrichment for.}

\item{toxprints}{Tibble/dataframe containing a chemical ID column and binary ToxPrint columns.}

\item{id_col_hitcalls}{<\code{\link[rlang:args_data_masking]{data-masked}}> Unquoted name of the
chemical ID column in \code{hitcalls}.}

\item{id_col_toxprints}{<\code{\link[rlang:args_data_masking]{data-masked}}> Unquoted name of the
chemical ID column in \code{toxprints}. Defaults to \code{id_col_hitcalls}.}

\item{endpoint_col_name}{Name of the column added to output identifying the assay. Default is "assay".}

\item{workers}{Number of parallel workers to use for assay-level parallelism. Default is \code{1} (sequential).
Set to a value greater than 1 to enable parallel processing via \code{future::multisession}. Alternatively,
set a \code{future::plan()} before calling this function for more control over the parallel backend.}

\item{...}{Additional arguments passed to \code{chemotype_enrichment()} (e.g. \code{alt_hypothesis}, \code{adjust_method}, \code{combo_sizes}).
Note: \code{workers} in \code{...} is ignored to prevent nested parallelism; use the \code{workers} argument directly.}
}
\description{
Wrapper around \code{chemotype_enrichment()} that iterates over multiple assays.
Assays with no tested chemicals, or where all tested chemicals are negative, are silently skipped.

Progress can be reported by wrapping the call in \code{progressr::with_progress()}.
Note: \code{workers} here parallelises across assays. Each assay's internal chemotype processing
always runs sequentially to avoid nested parallelism.
}
